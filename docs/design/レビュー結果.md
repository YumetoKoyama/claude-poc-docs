# レビュー結果（design）

> 最新 round が最上部。各 round は機械可読 JSON を人間向けに整形したもの。

## Round ? — 2026-07-14 00:32 — overall: FAIL（BLOCK 0 / SUGGEST 0 / NIT 0）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| — | — | — | 指摘なし | — | — |

検査済み観点: checked 44 / partial 0 / not-checked 1

未カバー領域:
- migration-consistency（not-checked）: check-migration-consistency.sh: migrationディレクトリ不在のためスキップ（デモ期間のddl-auto運用方針、backend-00-stack.md #8と整合する正当なスキップ）


## Round ? — 2026-07-13 07:30 — overall: FAIL（BLOCK 3 / SUGGEST 0 / NIT 0）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| BLOCK | traceability | docs/design/screens/SCR-009-案件詳細-配送依頼企業.md:85 | この画面は「関連機能要件」に4つの機能ファイル（交渉合意成約.md／運送ステータス報告.md／案件削除.md／案件登録.md）を挙げているが、「受け入れ条件」表のAC-IDが「AC-001」「AC-002」「AC-101」「AC-201」「AC-301」「AC-401」のように機能名で修飾されていない。shared-canon §1（親CLAUDE.md）の例外規定は「機能をまたいで参照する場合は必ず「機能名/AC-XXX」で修飾する」ことを要求しており、実際にAC-001／AC-101／AC-301／AC-401は4機能すべてに、AC-002は3機能に、AC-201は2機能に同一番号のACが存在するため、非修飾の「AC-001」等は読者にもRTM自動生成にも一意に解決できない（例: 1行目「AC-001」は交渉合意成約/AC-001を指すが4行目「AC-001」は案件削除/AC-001を指し、同一表内で同じIDが別のACを指す）。同一設計書内のSCR-006・SCR-013では正しく「案件登録/AC-201」「通知/AC-001」のように修飾されており、本画面のみ規約から逸脱している。 | 受け入れ条件テーブルの全AC-IDを「機能名/AC-XXX」形式に修飾する（例: 1行目→交渉合意成約/AC-001、2行目→交渉合意成約/AC-002、3行目→運送ステータス報告/AC-002、4行目→案件削除/AC-001、5行目→交渉合意成約/AC-101、6行目→案件削除/AC-101、7行目→運送ステータス報告/AC-102、8行目→交渉合意成約/AC-201、9行目→権限境界の由来機能で修飾、10行目→交渉合意成約/AC-401）。SCR-006・SCR-013の記法を参照する。 | 未対応 |
| BLOCK | traceability | docs/design/screens/SCR-015-案件詳細-運送会社.md:84 | この画面は「関連機能要件」に3機能（応募.md／交渉合意成約.md／運送ステータス報告.md）を挙げ、実際に2行目「応募/AC-001」・4行目「応募/AC-301」では正しく機能名修飾しているにもかかわらず、同じ表の1・3・5・6行目では「AC-001」「AC-101」「AC-301」「AC-401」を無修飾で使っており、これらの番号は応募.md／交渉合意成約.md／運送ステータス報告.md の3機能すべてに同名で存在する。特に5行目の無修飾「AC-301」（運送ステータス報告/AC-301を意図）は、4行目で既に修飾済みの「応募/AC-301」と同一表内に併存しており、AC-301という文字列が行ごとに異なるACを指すという内部矛盾が生じている。 | 1・3・5・6行目のAC-IDを「機能名/AC-XXX」に修飾する（1行目→運送ステータス報告/AC-001、3行目→運送ステータス報告/AC-101、5行目→運送ステータス報告/AC-301、6行目→運送ステータス報告/AC-401）。表内で修飾ありと修飾なしを混在させない。 | 未対応 |
| BLOCK | traceability | docs/design/screens/SCR-009-01-セット応募連動確認モーダル.md:58 | 「関連機能要件」に交渉合意成約.md と 応募.md の2機能を挙げているが、59行目の「AC-201」が無修飾。交渉合意成約.md にも応募.md にも AC-201 が存在し（内容は別物: 交渉合意成約/AC-201=セット応募の他社成約による不成立、応募/AC-201=セット応募時の応募枠消費）、行内容は交渉合意成約/AC-201に一致するが、ID表記だけでは一意に解決できない。 | 59行目のAC-IDを「交渉合意成約/AC-201」に修飾する。 | 未対応 |

検査済み観点: checked 42 / partial 3 / not-checked 1

未カバー領域:
- naming（partial）: コード値定義.md/データモデル.md/screens間の主要用語は一貫。全ファイル網羅的な英語名称揺れ検査は未実施
- validation（partial）: jobs.yaml/negotiations.yaml/tenants.yaml/users.yaml等主要APIで画面mdとの整合を抜き取り確認。全項目の突合はcheck-wiring-fields.shに依拠
- validation-pair（partial）: bids/jobs/negotiations/tenants/users yamlのmaxLength/pattern/requiredを画面md記載と抜き取り確認。全画面×全項目の突合はcheck-wiring-fields.shに依拠
- style（not-checked）: YAML/MDインデント揺れ・末尾空白の網羅的スキャンは未実施（validate-yaml-format.shのNIT検出範囲に限定）


## Round ? — 2026-07-13 06:56 — overall: FAIL（BLOCK 0 / SUGGEST 3 / NIT 0）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| SUGGEST | message-trace | docs/requirements/メッセージ一覧.md:35 | MSG-023の「トリガ」列は「自テナントが当事者でないデータへのアクセス・操作を試行（テナント越境）」と記載されているが、設計側（`セキュリティ設計.md` 5.1節「テナント越境は404」「自テナント内の権限不足は403」、`_common.yaml` ErrorCode.FORBIDDENのコメント、`フロントエンド共通設計.md` 3節「FORBIDDEN: テナント越境等は通常NOT_FOUNDになるため本コードは自テナント内の異常系のみ」）は一貫してFORBIDDEN(→MSG-023)を「自テナント内の権限不足」、テナント越境は404(→MSG-020/MSG-025)と定義しており、MSG-023の実発火条件と要件側のトリガ文言が逆になっている。 | 要件定義 phase へのフィードバックが必要: `メッセージ一覧.md` のMSG-023トリガ列を実際の403発火条件（例:「自テナント内で許可されない操作を試行」等、第1版では原則発生しない旨の注記付き）に修正する。 | 未対応 |
| SUGGEST | xss | docs/design/セキュリティ設計.md:121 | 要件側（`functional/案件登録.md`・`functional/交渉合意成約.md`・`functional/評価.md`）に自由入力フィールド（案件備考・メッセージ本文・評価コメント）を「プレーンテキスト」または「リッチHTML」に分類した記述が無い。`セキュリティ設計.md` 8節は「自由入力文字列はpattern等でHTMLタグの混入を制限する」という一般方針を述べ、実際に`jobs.yaml`の`remarks`・`negotiations.yaml`の`MessageCreateRequest.body`・`ratings.yaml`の`comment`すべてに`pattern: '^[^<>]*$'`が付与されており実務上はプレーンテキスト扱いで統一されているが、要件定義側にこの分類の明文化が無いため監査証跡としての追跡がしにくい。要件定義 phase へのフィードバックが必要。 | 要件`functional/案件登録.md`・`交渉合意成約.md`・`評価.md`の該当項目に「プレーンテキスト（HTMLタグ不可）」の分類を明記し、設計側は`セキュリティ設計.md`8節にその参照を追記する。 | 未対応 |
| SUGGEST | sequence | docs/design/sequences | `check-vertical-trace.sh`のWARNのとおり、`getBidById`・`getJobStatusSummary`・`getMyTenant`・`getSetBidById`・`listBidsForJob`・`listMessages`・`listMyBids`の7 operationIdがどの`sequences/*.md`にも登場しない。いずれも単純参照系（GET/list）で分岐業務ロジックを持たないため縦串断絶ではなく許容範囲だが、シーケンス一覧からの参照が一切無いため将来の監査で見落とされやすい。 | 各シーケンスファイルの「対応API」欄、または一覧性のあるインデックスに、これら参照系operationIdは「単純参照のため専用シーケンス省略」である旨を一行追記する。 | 未対応 |

検査済み観点: checked 40 / partial 5 / not-checked 2

未カバー領域:
- naming（partial）: 精読した範囲（tables/api/sequences）では英語表記の揺れは見当たらなかったが、全ファイルの網羅的な用語統一チェックは実施していない。
- validation（partial）: jobs.yaml(remarks maxLength2000/pattern)・negotiations.yaml(body minLength1/maxLength2000/pattern)をサンプル確認。全21画面×36 operationIdの網羅的な文字数・必須項目突合はcheck-wiring-fields.shの自動検出範囲に依拠し、個別画面mdの本文全件はLLMで精読していない。
- performance（partial）: tables/jobs.mdのインデックス方針、方式設計.md6節のスケーラビリティ方針(想定同時100ユーザー)を確認。BE実装レベルのN+1リスクは製造フェーズの検証対象であり設計書のスコープ外と判断。
- nonfunc-test（partial）: セキュリティテスト観点.md(認証・JWT改ざん節)を精読。非機能テスト計画.mdはセキュリティ・アクセシビリティ節を精読したが性能・可用性節は概要レベルの確認に留まる。
- validation-pair（partial）: jobs.yaml/negotiations.yamlのサンプル箇所のみ突合。全画面の網羅的突合は未実施（check-wiring-fields.shに依拠）。
- requirements-source-files（not-checked）: functional/*.md(10件)・activities/*.md(5件)・データモデル.md・用語集.md・移行要件.md・通知文面定義.md・非機能要件.md・ブランドガイドラインは個別に全文精読していない（他設計文書からの引用・突合による間接確認と決定論スクリプトのcontract/wiring-fieldsチェックに依拠）。
- remaining-tables-sequences-screens（not-checked）: tables/(contract_snapshots, final_offers, job_attributes, messages, password_reset_tokens, refresh_tokens, tenants, users)・sequences(交渉合意成約/セット応募一括合意以外の11件)・screens(共通レイアウト以外の20件)は個別mdを精読しておらず、check-db-design-consistency.sh/check-vertical-trace.sh/check-wiring-fields.shの決定論チェックとサンプル精読による外挿判断。


## Round ? — 2026-07-13 05:53 — overall: FAIL（BLOCK 0 / SUGGEST 2 / NIT 0）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| SUGGEST | message-trace | docs/design/フロントエンド共通設計.md:42-49 | 要件「メッセージ一覧.md」（MSG-001〜MSG-023）に対応するIDが無い表示文言が3件、ErrorCode→MSGマッピング表に「MSG 未割当」として残っている: (1) `NOT_FOUND` の通知遷移以外の汎用文言、(2) `DUPLICATE` の法人重複エラー（AC-201、アカウント登録.md／SCR-002）、(3) `RATE_LIMITED` のレート制限文言。他の全エラーコードはMSG-001〜MSG-023のカタログで文言を一元管理しているのに対し、この3件だけはサーバーの生message文字列やハードコードされた文言に依存しており、文言の一貫性・多言語化・要件トレーサビリティ（shared-canon §2 AC-XXXごとの追跡）の観点でカタログ方式の例外になっている。特にAC-201（法人重複）は要件のACに明記された「重複エラーが表示され」の具体文言が未確定のまま設計に取り込まれている。 | 要件フェーズへのフィードバックが必要: 「メッセージ一覧.md」に MSG-024（法人重複用）・MSG-025（汎用データ未検出用）・MSG-026（レート制限用）等を追補し、「フロントエンド共通設計.md」のマッピング表を「MSG 未割当」から正式な MSG-ID 参照に置き換える。要件追補が難しい場合は、当面の暫定文言であることと確定時期を「フロントエンド共通設計.md」に明記する。 | 未対応 |
| SUGGEST | naming | docs/design/api/set-bids.yaml:126 | `SetBidResponse.status` の enum（`[PENDING, CONTRACTED, FAILED]`）が `_common.yaml` を `$ref` せずインライン定義されている。同じ設計内の `JobStatus`（jobs.yaml）・`BidStatus`／`FinalOfferStatus`（bids.yaml）はいずれも `_common.yaml` の共通スキーマを `$ref` しており、この1件だけが例外になっている（`tables/set_bids.md` の CHECK 制約値 `'PENDING','CONTRACTED','FAILED'` と値集合は一致しているため機能的な不整合ではないが、コード値の管理箇所が分散している）。 | `_common.yaml` に `SetBidStatus` スキーマを追加し、`set-bids.yaml` から `$ref: "./_common.yaml#/components/schemas/SetBidStatus"` で参照するよう統一する。 | 未対応 |

検査済み観点: checked 44 / partial 0 / not-checked 1

未カバー領域:
- migration-consistency（not-checked）: check-migration-consistency.sh: migrationディレクトリ不在のためスキップ（デモ期間のddl-auto運用方針、backend-00-stack.md #8と整合する正当なスキップ）


## Round ? — 2026-07-13 05:22 — overall: FAIL（BLOCK 0 / SUGGEST 1 / NIT 0）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| SUGGEST | message-trace | docs/design/フロントエンド共通設計.md:42 | 要件「メッセージ一覧.md」に AC-201（企業アカウント登録の法人重複エラー）に対応する MSG-XXX が存在しないため、`ErrorCode.DUPLICATE`（`registerTenant` の 409）の表示文言が「サーバー message をそのまま表示（AC-201、MSG 未割当）」という暫定運用のまま設計に取り込まれている。`functional/アカウント登録.md` の入力項目表は「登録結果メッセージ \| 文字列（MSG-XXX）」と MSG-XXX の存在を前提にしているが、`メッセージ一覧.md`（MSG-001〜023）には該当行が無く、要件側の定義漏れが設計にそのまま持ち越されている。同様に `NOT_FOUND` の汎用文言・`RATE_LIMITED`・`INTERNAL_ERROR` も「MSG 未割当」の暫定文言のまま。 | 要件定義 phase へのフィードバックが必要。`メッセージ一覧.md` に法人重複エラー用の MSG-XXX（例:「この法人名・住所の企業アカウントは既に登録されています。」）を追加し、`functional/アカウント登録.md` AC-201 および `フロントエンド共通設計.md` 3節・`api/tenants.yaml` の `registerTenant` 409 記述から参照する。汎用 404/429/500 文言は業務メッセージでなくシステム共通文言のため MSG 未割当のままでも許容可だが、AC-201 分は要件不足であるため優先対応する。 | 未対応 |

検査済み観点: checked 42 / partial 3 / not-checked 1

未カバー領域:
- naming（partial）: コード値定義.md/データモデル.md/screens間の主要用語は一貫。全ファイル網羅的な英語名称揺れ検査は未実施
- validation（partial）: jobs.yaml/negotiations.yaml等主要APIで画面mdとの整合を抜き取り確認。全項目の突合はcheck-wiring-fields.shに依拠
- validation-pair（partial）: bids/jobs/negotiations yamlのmaxLength/pattern/requiredを画面md記載と抜き取り確認。全画面×全項目の突合はcheck-wiring-fields.shに依拠
- style（not-checked）: YAML/MDインデント揺れ・末尾空白の網羅的スキャンは未実施（validate-yaml-format.shのNIT検出範囲に限定）


## Round ? — 2026-07-13 03:07 — overall: FAIL（BLOCK 0 / SUGGEST 3 / NIT 0）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| SUGGEST | concurrency | docs/design/sequences/交渉合意成約.md:93-101 | 単体応募の合意処理（agreeFinalOffer）で、同一案件内の他応募（set_bid_id を持つ）をクローズする際、その set_bid_id に連動する「他案件」側の応募（bids WHERE set_bid_id=... AND status='OPEN'）を、対応する jobs 行を FOR UPDATE でロックせずに直接 UPDATE している。`sequences/セット応募一括合意.md` は同じ BR-015 連動クローズシナリオに対し Phase1（非ロック洗い出し）→Phase2（対象job_id昇順で一括 FOR UPDATE）という明示的な2段階ロック手順を踏んでおり、同一業務ルール（BR-015）の実装なのに両シーケンス間でロック取得順序の方針が非対称になっている。概要.md 4節のリスク#2は「ロック順序を誤るとデッドロックが発生し得る」と明記しており、この非対称性が意図的な安全策（他ジョブの jobs.status 自体は変更しないため不要）なのか設計漏れなのかが記述からは判別できない。セキュリティテスト観点.md 5節#4のデッドロック検証も「セット応募一括合意」のみを対象にしており、単体応募からの連動クローズ経路は検証対象外のまま。 | 交渉合意成約.md に「connected job のロックが不要である理由（他ジョブの jobs.status は変更せず bids/set_bids のみ更新するため）」を明記するか、セット応募一括合意.md と同様の ID 昇順一括ロックへ統一する。あわせて `セキュリティテスト観点.md` 5節#4の検証対象に単体応募からの連動クローズケースを追加する。 | 未対応 |
| SUGGEST | message-trace | docs/requirements/メッセージ一覧.md:35 | MSG-023 のトリガ記述は「自テナントが当事者でないデータへのアクセス・操作を試行（テナント越境）」だが、設計（セキュリティ設計.md 5.1・5.3、_common.yaml ErrorCode.FORBIDDEN のコメント、フロントエンド共通設計.md 3節）は一貫して「テナント越境は404 NOT_FOUND」「403 FORBIDDEN は自テナント内の権限不足（第1版では原則発生しない）」と定義しており、MSG-023＝FORBIDDEN の実際の発火条件と要件側のトリガ文言（テナント越境）が逆になっている。 | 要件定義 phase へのフィードバックが必要: `メッセージ一覧.md` の MSG-023 トリガ列を「自テナント内で許可されない操作（ロール種別不一致等）を試行」等、実際の403発火条件に合わせて修正する。テナント越境（404）時にユーザーへ表示する汎用文言が別途必要であれば新規 MSG-ID を要件側で採番する。 | 未対応 |
| SUGGEST | message-trace | docs/design/フロントエンド共通設計.md:42-49 | ErrorResponse.code→表示文言マッピング表で、NOT_FOUND（汎用）・DUPLICATE（法人重複）・RATE_LIMITED・INTERNAL_ERROR の4コードの表示文言が「MSG未割当」と自己申告されており、要件 `メッセージ一覧.md` に対応行が無いまま設計側で暫定文言を新設している。 | 要件定義 phase へのフィードバックが必要: `メッセージ一覧.md` に当該4パターンの MSG-XXX（例: MSG-024〜027）を追記し、本ファイルの「MSG未割当」注記を確定 MSG-ID 参照に置き換える。 | 未対応 |

検査済み観点: checked 44 / partial 3 / not-checked 0

未カバー領域:
- validation-pair（partial）: SCR-014/jobs.yaml等サンプル箇所のみLLMで精読突合。全21画面×全36operationIdの網羅的な文字数・必須項目の1:1突合はcheck-wiring-fields.shの自動検出範囲に依拠し、個別画面の詳細本文全件はLLMで精読していない。
- performance-n1（partial）: 一覧系API(listJobs等)のページング設計は確認したが、BE実装のJOIN/バッチフェッチ戦略は方式設計.md6節の方針レベルに留まり、実装詳細レベルのN+1リスクは製造フェーズでの検証に依存（設計書のスコープ外）。
- requirements-boundary（partial）: docs/requirements/外部インターフェース一覧.md がgit未コミットの変更（EXT-011行追加）を含んでいることを確認したが、内容自体はclosed済みQ-NF10の決定を反映したものであり数値矛盾は無い。設計フェーズが要件ファイルを直接編集した経緯かどうかは本レビューの対象範囲外（人手採択プロセスで確認要）。


## Round ? — 2026-07-13 02:02 — overall: FAIL（BLOCK 2 / SUGGEST 1 / NIT 1）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| BLOCK | data-sufficiency | docs/design/api/bids.yaml:190 | 業務ルール「BR-020」は「相手企業の電話番号・メールアドレスは募集中・交渉中でも表示可」と双方向で定めるが、設計は配送依頼企業→運送会社方向（jobs.yaml JobDetailResponse.requesterCompanyPhone/requesterCompanyEmail）のみを実装しており、逆方向（配送依頼企業がSCR-009で応募一覧から運送会社の会社電話番号・メールアドレスを見る）の供給元が存在しない。BidResponse/BidWithJobSummaryResponseにはcarrierCompanyNameのみでcarrierCompanyPhone/carrierCompanyEmailが無く、tenants.yamlのgetMyTenantも自社限定のため他社情報を取得できない。 | bids.yamlのBidResponseにcarrierCompanyPhone/carrierCompanyEmail（他社運送会社への匿名化時はnull、BR-021と同様のマスク方針）を追加し、SCR-009-案件詳細-配送依頼企業.mdの「出力・表示内容」表に応募一覧の会社連絡先項目を明記する。 | 未対応 |
| BLOCK | nonfunc-traceability | docs/design/運用設計.md:48 | 要件で確定済み（Q-NF10、データモデル.md ENT-009「保持期間90日間、経過後は自動削除」）の非機能要件が、設計では実装として落ちていない。tables/notifications.mdは「90日後に物理削除バッチで削除」と実施済みであるかのように記述するが、バッチ設計.mdが定義する第1版の唯一のバッチはEXT-010（案件物理削除）のみで通知の物理削除バッチは存在せず、運用設計.md自身が「物理削除バッチは第1版では通知向けに個別実装せず、将来拡張として...追加する想定」と明記し実装を先送りしている。設計着手前クローズ必須の確定値（自動削除）が設計に反映されていない。 | バッチ設計.mdに通知（notifications）の90日経過後物理削除バッチを新設して2.x節に追加するか、EXT-010のスコープに含めて再設計する。実装しない場合はQ-NF10の再クローズ（要件差し戻し・要件採択者の再承認）が必要。tables/notifications.mdと運用設計.mdの記述を実装状況に合わせて整合させる。 | 未対応 |
| SUGGEST | naming | docs/design/api/set-bids.yaml:124 | SetBidResponse.statusがenum: [PENDING, CONTRACTED, FAILED]をインラインで定義しており、JobStatus/BidStatus等の他のコード値がすべて_common.yamlに集約されている方針（shared-canon §4 4層統一）と不揃いになっている。 | _common.yamlにSetBidStatusを追加し$refで参照するよう統一する（tables/set_bids.mdのCHECK制約値と一致させる）。 | 未対応 |
| NIT | style | docs/design/api/tenants.yaml:43 | operationId「getMyTenant」がどの画面設計（screens/*.md）からも呼び出されておらず、概要.md前提条件でも「参照のみを設計する」という将来拡張目的の記載に留まり、第1版で実際に使用されるAPIかが不明瞭。 | 現時点で利用画面が無いことを概要.mdまたはtenants.yamlのdescriptionに明記し、将来のテナント情報表示機能実装時に紐づけ先画面を追記する。 | 未対応 |

検査済み観点: checked 45 / partial 0 / not-checked 1

未カバー領域:
- migration-consistency（not-checked）: check-migration-consistency.sh: migrationディレクトリ不在のためスキップ（デモ期間のddl-auto運用方針と整合、正当なスキップ）


## Round ? — 2026-07-13 01:28 — overall: FAIL（BLOCK 2 / SUGGEST 1 / NIT 10）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| BLOCK | security-design | docs/design/sequences/応募.md:110 | 「テナント越境（編集）\| 他社の応募を編集 \| 403 FORBIDDEN」と記載されているが、`セキュリティ設計.md` §5.1 の統一方針（テナント越境は404・自テナント内の権限不足は403）および同 §5.3 の `updateBid` 行「自テナントの応募かつ成約前のみ（BR-019、他は404、成約後は409）」と矛盾する。`フロントエンド共通設計.md` 3節も「FORBIDDEN...テナント越境等は通常NOT_FOUNDになるため本コードは自テナント内の異常系のみ」と明記しており、他社の応募（テナント越境）を403として扱う本行は設計間で応答コードが不統一。 | 該当行を「テナント越境 \| 他社の応募を編集 \| 404 NOT_FOUND」に修正し、`セキュリティ設計.md` updateBid行・`フロントエンド共通設計.md` の記述と一致させる。403はロール不一致（例: 配送依頼企業ユーザーによる応募系操作）等の別行として維持する。 | 未対応 |
| BLOCK | security-design | docs/design/sequences/セット応募一括合意.md:93 | 「認可失敗 \| 対象案件群の配送依頼企業でないテナントによる呼び出し \| 403 FORBIDDEN」と記載されているが、発生条件はテナント境界（自テナントが当事者でない案件群への操作）であり、`セキュリティ設計.md` §5.1「テナント越境（自テナントが当事者でないデータへのアクセス）は404」の統一方針と矛盾する。`フロントエンド共通設計.md` 3節の FORBIDDEN 定義（自テナント内の異常系のみ）とも整合しない。 | 該当行を「テナント越境 \| 対象案件群の配送依頼企業でないテナントによる呼び出し \| 404 NOT_FOUND」に修正する。 | 未対応 |
| SUGGEST | naming | docs/design/screens/SCR-007-案件登録.md:56 | 「認可設計.md」という独立ファイルへの参照があるが、`セキュリティ設計.md` 冒頭の注記で「認可設計（operationId×ロール×テナント条件）は本ファイル5節に内包する（`認可設計.md`へは分割しない）」と明記されており、`認可設計.md` というファイルは実在しない（同様の誤参照が SCR-005/SCR-006/SCR-009/api/jobs.yaml/api/users.yaml の計6箇所に存在）。 | 「認可設計.md」への参照をすべて「セキュリティ設計.md（5節 認可設計）」に統一する。 | 未対応 |
| NIT | style | docs/design/api/_common.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/_common.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/auth.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/auth.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/bids.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/bids.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/jobs.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/jobs.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/negotiations.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/negotiations.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/notifications.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/notifications.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/ratings.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/ratings.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/set-bids.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/set-bids.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/tenants.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/tenants.yaml を実行してフォーマットを自動修正してください。 | 未対応 |
| NIT | style | docs/design/api/users.yaml | YAML フォーマット違反: Prettier の規約に従っていません（インデント幅・クォート・末尾スペース等）。 | npx prettier --write docs/design/api/users.yaml を実行してフォーマットを自動修正してください。 | 未対応 |

検査済み観点: checked 40 / partial 2 / not-checked 0

未カバー領域:
- param-source（partial）: 主要画面(SCR-009, SCR-015, SCR-016等)のパス変数(jobId, bidId等)の出所を画面遷移・API対応表でサンプル確認したが全画面の網羅的突合はスクリプト未整備のため一部手動確認に留まる。
- validation-pair（partial）: 代表画面(SCR-007)で確認したがmaxLength等の具体的桁数は画面mdに明記されない設計方針（API側を正典とする）であり、全画面の網羅的突合はスクリプト未整備。


## Round ? — 2026-07-13 00:50 — overall: FAIL（BLOCK 2 / SUGGEST 2 / NIT 1）

| 重大度 | カテゴリ | 該当 | 指摘 | 推奨対応 | 対応状況 |
|---|---|---|---|---|---|
| BLOCK | completeness | claude-poc-docs/docs/design/sequences/ | 要件の業務アクティビティ「案件削除フロー」（ACT-005、requirements/activities/案件削除フロー.md）は「ステータス判定→論理削除→応募運送会社への通知(MSG-013)→夜間バッチでの物理削除」という分岐を持つフローだが、対応する sequences/*.md が1件も存在しない（sequences配下11ファイルのいずれにもdeleteJobが登場しない、check-vertical-trace.shのWARNでも検出済み）。バッチ内部のトランザクション設計はバッチ設計.mdにあるが、deleteJob呼び出し時点の同期処理（ステータスガード判定・論理削除・同一トランザクション内通知作成失敗時の扱い）を図示したシーケンスが欠落している。 | sequences/案件削除.md を新規作成し、deleteJob（jobs.yaml）のステータスガード判定・論理削除・応募運送会社への通知（MSG-013、通知失敗時も操作自体は成功させる旨）をシーケンス図で明示する。ACT-005・BR-022・SCR-009と対応づける。 | 未対応 |
| BLOCK | vertical-trace | claude-poc-docs/docs/design/api/jobs.yaml:158 | operationId「updateJob」（案件編集、PATCH /api/jobs/{jobId}）が sequences/*.md のいずれにも登場しない（check-vertical-trace.shのWARNで検出）。縦串整合チェックリスト2節(d)「CRUD系operationIdはシーケンスが存在すること（必須）」に違反する。tables/jobs.mdの排他制御節には楽観ロック方針の記載があるが、更新フロー自体を図示したシーケンスが無い。 | updateJobを含むシーケンス（既存のsequences/案件登録.mdへの追記、または新規sequences/案件編集.mdの作成）を追加し、BR-019のステータスガード判定と楽観ロック競合時の409応答を図示する。 | 未対応 |
| SUGGEST | message-trace | claude-poc-docs/docs/requirements/メッセージ一覧.md:35 | MSG-023のトリガ記述は「自テナントが当事者でないデータへのアクセス・操作を試行（テナント越境）」だが、shared-canon（親CLAUDE.md）は「テナント越境は404、自テナント内の権限不足は403」と統一を義務付けており、設計側（セキュリティ設計.md 5.3、フロントエンド共通設計.md 3節）もこの方針に従いFORBIDDEN(403)→MSG-023を「ロール種別不一致等の自テナント内異常系」専用としている（テナント越境は404でNOT_FOUND系メッセージ）。要件のMSG-023トリガ文言が実際の発火条件（テナント越境ではなくロール越境）と一致しておらず、要件定義phaseへのフィードバックが必要。 | メッセージ一覧.mdのMSG-023トリガ列を「自テナント内で許可されない操作（ロール種別不一致等）を試行」等、実際の403発火条件に合わせて修正する。テナント越境（404）用の汎用メッセージが別途必要であれば新規MSG番号を要件側で採番する。 | 未対応 |
| SUGGEST | message-trace | claude-poc-docs/docs/design/フロントエンド共通設計.md:42 | ErrorResponse.code→表示文言マッピング表で、NOT_FOUND（汎用）・DUPLICATE（法人重複）・RATE_LIMITED・INTERNAL_ERRORの4コードが「MSG未割当」と自己申告されており、要件メッセージ一覧.mdに対応行が無い。要件定義phaseへのフィードバック（メッセージ一覧.mdへのMSG-024以降の追記）が必要。 | メッセージ一覧.mdに当該4パターンのMSG-XXXを追記し、フロントエンド共通設計.mdの「MSG未割当」注記を解消する。 | 未対応 |
| NIT | style | claude-poc-docs/docs/design/セキュリティ設計.md:100 | 5.3節末尾の注記「全 33 operationId を網羅（api/*.yaml の operationId: 合計と一致）」が実際のoperationId総数（36、check-contract.shのINFO出力および api/*.yaml の operationId: grep countで確認）と不一致。表自体は36行あり実質網羅されているが、注記の数値のみ誤り。 | 「全 33 operationId」を「全 36 operationId」に修正する。 | 未対応 |

検査済み観点: checked 44 / partial 2 / not-checked 0

未カバー領域:
- validation-pair（partial）: SCR-007/jobs.yaml等サンプル箇所のみ突合。全21画面×全36operationIdの网羅的な文字数・必須項目の1:1突合はcheck-wiring-fields.shのSUGGEST検出範囲に依拠し、個別画面の詳細本文まではLLMで全件精読していない。
- performance-n1（partial）: 一覧系API(listJobs等)のページング設計は確認したが、BE実装のJOIN/バッチフェッチ戦略はdocs/design記述の範囲(方式設計.md6節スケーラビリティ方針)に留まり、実装詳細レベルのN+1リスクは製造フェーズでの検証に依存。

