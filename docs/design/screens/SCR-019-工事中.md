# 画面設計: SCR-019 工事中画面（問い合わせ案内）

> ID 凡例: [docs/凡例.md](../../凡例.md) 参照

## 画面概要

- 画面 ID: SCR-019
- URL パス: `/under-construction`
- 対応ユースケース: なし（本格的な問い合わせフォーム未整備時の案内、`非機能要件.md`「サポート・利用停止」節参照）
- 関連機能要件: なし（要件定義に専用の functional ファイルが存在しないため、本画面の受け入れ条件は画面概要から新規に画面スコープで採番する。`docs/requirements/画面一覧.md` SCR-019 の記述を参照）
- 所属レイアウト: 共通シェル

## 入力

なし（案内表示のみ）。

## 出力・表示内容

| 項目名 | 型 | データ源（operationId.フィールド） | 説明 |
|-------|----|--------------------------|------|
| 案内文言 | 文字列 | 静的コンテンツ（API 呼び出しなし） | 不具合報告・問い合わせ導線の簡易案内 |

## バリデーションメッセージ

なし。

## 画面遷移（この画面から）

| トリガー | 遷移先（画面 ID） | 条件 |
|---------|--------------------|------|
| 「戻る」 | 直前の画面 | ブラウザ履歴 or 共通ヘッダーのダッシュボードリンク |

## API 対応

なし（静的コンテンツのみで API 呼び出しを行わない）。

## セキュリティ考慮事項

- 認証: 必要（共通シェル配下のため）
- 認可: 全ロール共通で表示可
- CSRF 対策: 対象外（API 呼び出しなし）
- その他: なし

## 受け入れ条件

> 関連する機能要件ファイルが存在しないため、画面概要（画面一覧.md SCR-019）から画面スコープで新規採番する（`SCR-019/AC-XXX`）。

| AC-ID | 区分 | Given（前提状態） | When（操作） | Then（期待結果） | 関連 BR | operationId |
|-------|------|-----------------|------------|----------------|--------|-------------|
| SCR-019/AC-001 | 正常系 | 共通ヘッダーの「お問い合わせ」導線が表示されている状態 | 「お問い合わせ」をクリックする | 工事中画面へ遷移し案内文言が表示される | — | — |
| SCR-019/AC-002 | 正常系 | 工事中画面を表示中の状態 | 「戻る」を操作する | 直前の画面へ戻る | — | — |
